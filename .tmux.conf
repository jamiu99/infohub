set -g mouse on                 # 鼠标滚动 + 点击切换窗格
set -g history-limit 50000
set -g pane-border-status top   # 窗格顶部显示标题
set -g pane-border-format " #{pane_index}: #{pane_title} "
set -g pane-active-border-style fg=green
